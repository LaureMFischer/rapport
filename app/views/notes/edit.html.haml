%nav.navbar.navbar-default.navbar-fixed-top{ :role => 'navigation' }
  .container-fluid
    .navbar-header
      %button.navbar-toggle{ :type => "button", :data => {:toggle => "collapse", :target => "#bs-example-navbar-collapse-1"}}
        %span.sr-only Toggle navigation
        %span.icon-bar
        %span.icon-bar
        %span.icon-bar
      %p.navbar-text.brand <em>Rapport</em>

    .collapse.navbar-collapse#bs-example-navbar-collapse-1
      %ul.nav.navbar-nav
        %li= link_to 'Contact Queue', connections_path
        %li= link_to 'All Contact Notes', connection_notes_path(@connection.id)
        %li= link_to "Logout", destroy_user_session_path, method: :delete

      %ul.nav.navbar-nav.navbar-right
        %li
          %p.navbar-text Logged in

= form_for [@connection, @note] do |form|
  .form-label
    = form.label 'Note'
  .form-input
    = form.text_area :body
  .form-label
    = form.label :date_of_interaction
  .form-input
    = form.text_field :date_of_interaction
  = form.submit "Update Note"
