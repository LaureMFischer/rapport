<nav class="navbar navbar-default navbar-fixed-top" role="navigation">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <p class="navbar-text brand"><em>Rapport</em></p>
    </div>

    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li><%= link_to 'Contact Queue', connections_path %></li>
        <li><%= link_to "Logout", destroy_user_session_path, method: :delete %></li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <li><p class="navbar-text">Logged in as <strong><%= current_user.email %></strong></p></li>
      </ul>
    </div>
  </div>
</nav>

<% if user_signed_in? %>
<h2><%= @connection.first_name + ' ' + @connection.last_name %></h2>
<h3><%= @connection.headline %></h3>
<div class="panel panel-success">
  <div class="panel-heading">Notes<button class="new-button btn-sm" type="button"><%= link_to "Add New Note", new_connection_note_path(@connection.id) %></button></div>
  <table class="table table-hover">
    <tr>
      <th>Note</th>
      <th>Date of Interaction</th>
      <th></th>
    </tr>
    <tr>
    <% if @notes.nil? == false %>
      <% @notes.each do |note| %>
      <td><%= note.body %></td>
      <td><%= note.date_of_interaction %></td>
      <td><button type="button"><span class="glyphicon glyphicon-pencil"></span><%= link_to " Edit", edit_connection_note_path(note.connection_id, note.id) %></button></td>
      </tr>
      <% end %>
  </table>
  <% else %>
  <% end %>
<% else %>
<% end %>
