<nav class="navbar navbar-default navbar-fixed-top" role="navigation">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <p class="navbar-text brand"><em>Rapport</em></p>
    </div>

    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li><%= link_to 'Contact Queue', connections_path %></li>
        <li><%= link_to "Logout", destroy_user_session_path, method: :delete %></li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <li><p class="navbar-text">Logged in as <strong><%= current_user.email %></strong></p></li>
      </ul>
    </div>
  </div>
</nav>

<%= form_for @connection do |form| %>
  <div class="form-label"><%= form.label :first_name %></div>
  <div class="form-input"><%= form.text_field :first_name %></div>

  <div class="form-label"><%= form.label :last_name %></div>
  <div class="form-input"><%= form.text_field :last_name %></div>

  <div class="form-label"><%= form.label 'Current Position' %></div>
  <div class="form-input"><%= form.text_field :headline %></div>

  <div class="form-label"><%= form.label 'Date to contact next' %></div>
  <div class="form-input"><%= form.text_field :date %></div>
  <%= form.submit "Update" %>
<% end %>

<button class="add-a-note"><%= link_to "Add a Note to this Contact", new_connection_note_path(@connection.id) %></button>
